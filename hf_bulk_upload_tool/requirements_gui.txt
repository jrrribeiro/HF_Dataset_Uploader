# Requirements for the GUI version of HF Bulk Uploader
# These are IN ADDITION to the base requirements.txt

# GUI Framework
customtkinter==5.2.2

# Base requirements (should already be installed)
# huggingface-hub>=0.19.0
# requests>=2.31.0
# tqdm>=4.65.0
