# Archive

This folder is reserved for legacy planning documents, old release notes, and other materials that are not part of the active `hf_upload_script` and `hf_upload_app` workflows.

The active project structure lives at the repository root in:
- `hf_upload_script/`
- `hf_upload_app/`

Current root documentation is intentionally kept minimal.
